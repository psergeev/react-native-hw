import { StyleSheet } from 'react-native';
import { theme } from '../../theme';

export const AppHeaderStyles = StyleSheet.create({
    text: {
        color: theme.primaryColors.graphite,
        fontFamily: theme.fonts.funny,
        fontSize: 50
    }
});