import { StyleSheet } from 'react-native';

export const ProductsScreenStyles = StyleSheet.create({
    header: {
        marginTop: 20,
        marginBottom: 20,
        textAlign: 'center'
    }
});